{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CMake: Debug (lldb)",
      "type": "lldb",
      "request": "launch",

      // CMake Tools が教えてくれる実行ファイルパス
      "program": "${command:cmake.launchTargetPath}",

      "args": [],
      "cwd": "${workspaceFolder}",
      "terminal": "integrated",
      "stopOnEntry": false
    }
  ]
}